name: plex
summary: A Plex Media Server K8s charm
description: |
    Plex Media Server
series:
   - kubernetes
min-juju-version: 2.7.5
storage:
  config:
    type: filesystem
    location: /config
  transcode:
    type: filesystem
    location: /transcode
  data:
    type: filesystem
    location: /data
deployment:
  type: stateful
  service: cluster
